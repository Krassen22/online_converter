<div class="form"> 
	<div class="form-title"><%= @converter.name %></div>	
		<%= form_for @request do |f| %>
				<div>
					<%= f.label :source_url %>
					<%= f.text_field :source_url, value: "http://www.online-image-editor.com/styles/2014/images/example_image.png" %>
				</div>
				<div>
					<%= f.label :convert_to %>
					<%= f.select :convert_to, options_from_collection_for_select( @converter.formats, "format", "format" ) %>
				</div>
				<%= f.submit %>
		<% end %>
</div>
